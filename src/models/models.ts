export interface Todo {
  id: number;
  todo: string;
  completed: boolean;
}

export interface Button {
  label: string;
}

export interface Theme {
  themeName: 'light' | 'dark'
}


export interface Todo {
  title: string,
  id: number,
  completed: boolean,
}